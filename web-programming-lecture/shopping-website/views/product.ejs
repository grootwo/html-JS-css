<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <title>
        <%= product.name %>
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <style>
        .information {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row">
            <!-- Product Images -->
            <div class="col-md-6 mb-3">
                <img src="<%= product.thumbnail %>" alt="Product" class="img-fluid rounded product-image"
                    id="mainImage">
            </div>

            <!-- Product Details -->
            <div class="col-md-6 information">
                <div>
                    <h2 class="mb-3">
                        <%= product.name %>
                    </h2>
                    <p class="mb-4">
                        <%= product.description %>
                    </p>
                </div>
                <div>
                    <div class="mb-3">
                        <p><i class="bi bi-star-fill"></i> 키우기 난이도</p>
                        <% for (var i=0; i < product.difficulty; i++){%>
                            <i class="bi bi-star-fill text-warning"></i>
                            <% } %>
                                <% for (var i=0; i < 5 - product.difficulty; i++){%>
                                    <i class="bi bi-star text-warning"></i>
                                    <% } %>
                                        <p><i class="bi bi-heart-fill"></i> 좋아요 <%= product.likes %>
                                        </p>
                    </div>
                </div>
                <div>
                    <div class="mb-3">
                        <span class="h5 me-2">
                            <%= product.price.toLocaleString() %>원
                        </span>
                    </div>
                    <div class="mb-4">
                        <label for="quantity" class="form-label">수량:</label>
                        <input type="number" class="form-control" id="quantity" value="1" min="1" style="width: 80px;">
                    </div>
                    <button class="btn btn-primary btn-lg mb-3 me-2">
                        <i class="bi bi-cart-plus"></i> 장바구니에 담기
                    </button>
                    <button class="btn btn-outline-secondary btn-lg mb-3">
                        <i class="bi bi-heart"></i> 위시리스트에 담기
                    </button>
                </div>
            </div>

            <img src="<%= product.descriptionImage %>">
        </div>
    </div>
</body>