<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <title>
        <%= product.name %>
    </title>
    <style>
        .information {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row">
            <!-- Product Images -->
            <div class="col-md-6 mb-3">
                <img src="<%= product.thumbnail %>" alt="Product" class="img-fluid rounded product-image"
                    id="mainImage">
            </div>

            <!-- Product Details -->
            <div class="col-md-6 information">
                <div>
                    <h2 class="mb-3">
                        <%= product.name %>
                            <% if (product.is_featured===1) { %>
                                <i class="bi bi-stars text-warning isFeaturedIcon"></i>
                                <% } %>
                    </h2>
                    <% if (product.is_featured===1) { %>
                        <p class="text-muted mb-4">이번 계절 추천 식물</p>
                        <% } %>
                            <p class="mb-4">
                                <%= product.description %>
                            </p>
                </div>
                <div>
                    <div class="mb-3">
                        <p><i class="bi bi-star"></i> 키우기 난이도: <%= product.difficulty %>
                        </p>
                        <p><i class="bi bi-heart"></i> 좋아요: <%= product.likes %>
                        </p>
                    </div>
                </div>
                <div>
                    <div class="mb-3">
                        <span class="h5 me-2">
                            <%= product.price.toLocaleString() %>원
                        </span>
                    </div>
                    <!-- <div class="mb-4">
                        <label for="quantity" class="form-label">수량:</label>
                        <input type="number" class="form-control" id="quantity" value="1" min="1" style="width: 80px;">
                    </div> -->
                    <form action="/wish/add" method="POST">
                        <input type="hidden" name="productId" value="<%= product.id %>">
                        <button class="btn btn-outline-secondary btn-lg mb-3" type="submit">
                            <i class="bi bi-heart"></i> 위시리스트에 담기
                        </button>
                    </form>
                    <form action="/cart/add" method="POST">
                        <input type="hidden" name="productId" value="<%= product.id %>">
                        <button class="btn btn-primary btn-lg mb-3 me-2" type="submit">
                            <i class="bi bi-cart-plus"></i> 장바구니에 담기
                        </button>
                    </form>
                </div>
            </div>

            <img src="<%= product.descriptionImage %>">
        </div>
    </div>
</body>